package se.liu.antbe028.pacman;

/** BoardListener is a interface that updates the board every tick,
 * it's major responsibility is to repaint the board.
 */

public interface BoardListener {

    public void boardChanged();
    
}
